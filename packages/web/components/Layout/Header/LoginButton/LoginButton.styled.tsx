import styled from 'styled-components';

export const Button = styled.button`
  background: #fff;
  border: 0 none;
  display: flex;
  align-items: center;

  &:hover {
    cursor: pointer;
  }
`;

export const Icon = styled.img`
  margin-right: 0.5rem;
`;
